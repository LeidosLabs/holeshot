To install requirements in project folder (for deployment zip file)
pip install -r /path/to/project/requirements.txt -t /path/to/project/

To lock in the version of boto3 being used, add it to requirements .txt before performing the install